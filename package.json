{
  "name": "rrm-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "test:rules": "vitest run --config vitest.rules.config.js",
    "test:rules:emulator": "npx -y firebase-tools@latest emulators:exec --only firestore \"npm run test:rules\"",
    "deploy": "npm run build && npx -y firebase-tools@latest deploy --only hosting:default-site --non-interactive",
    "deploy:verify": "npm run deploy",
    "deploy:prod": "npm run build && npx -y firebase-tools@latest deploy --only hosting:pons-site --non-interactive",
    "deploy:slot": "npm run build && npx -y firebase-tools@latest deploy --only hosting:slot-site --non-interactive"
  },
  "dependencies": {
    "firebase": "^12.12.1",
    "framer-motion": "^12.40.0",
    "lucide-react": "^0.511.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-is": "^19.2.7",
    "recharts": "^3.8.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "@vitejs/plugin-react": "^4.3.4",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.3",
    "tailwindcss": "^3.4.17",
    "vite": "^4.5.14",
    "vitest": "^4.1.5"
  }
}
